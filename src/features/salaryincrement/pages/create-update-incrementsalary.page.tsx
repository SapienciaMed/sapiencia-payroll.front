import React from "react";

const CreateUpdateIncrementSalary = (): React.JSX.Element => {
  return <div>create-update-incrementsalary.page</div>;
};

export default React.memo(CreateUpdateIncrementSalary);
